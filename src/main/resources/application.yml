server:
  port: 9080
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/dev?serverTimezone=UTC&useSSL=false&useUnicode=true&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: ENC(KHS5zGygMlA3HE/63R5cZg==)
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.sun.foot.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.sun.foot.mapper: DEBUG

encryption:
  key:
    path: /app/config/secret.key   # 绝对路径或相对路径

snowflake:
  datacenter-id: 1       # 机房A